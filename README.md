# NumPy Practice

This repository contains my NumPy practice and learning notes in Python.

## Topics Covered

* NumPy Array
* `dtype`
* `ndim`
* `shape`
* `reshape()`
* `flatten()`
* `ndmin`
* `arange()`
* `linspace()`
* `random.randint()`
* `random.randn()`
* `random.rand()`
* `random.choice()`
* `random.shuffle()`
* `copy()`
* `view()`
* `zeros()`
* `ones()`

## Description

This notebook contains examples and practice programs to understand NumPy arrays and commonly used NumPy functions.

The examples cover different types of arrays, dimensions, shapes, reshaping, flattening, generating sequences, generating random values, and understanding the difference between `copy()` and `view()`.

## Technologies Used

* Python
* NumPy
* Jupyter Notebook

## How to Run

1. Install Python.
2. Install NumPy:

```bash
pip install numpy
```

3. Open `Numpy.ipynb` in Jupyter Notebook or VS Code.
4. Run the cells one by one.

## File

`Numpy.ipynb` – Contains NumPy concepts, examples, and practice code.

## Purpose

The purpose of this notebook is to practice NumPy fundamentals and build a strong foundation for Python, Data Analysis, and Machine Learning.
